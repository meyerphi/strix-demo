{
    "inputs": ["locked", "hready"],
    "outputs": ["hmastlock"],
    "assumptions": [],
    "guarantees": [
        "G (hready -> (X hmastlock <-> locked))",
        "G (!hready -> (X hmastlock <-> hmastlock))"
    ]
}
